# Pré-Requisitos
- Compilador de C++ (ex: gcc)
- bash

# Inicializar Programa

Basta rodar o arquivo executável __run_teste.sh__ na pasta principal (/trabalho-grafos) com:

~~~ bash
./run_teste.sh
~~~